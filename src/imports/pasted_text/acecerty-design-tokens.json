You are an elite UI/UX Designer and Frontend Architect creating a multi-page, premium, high-converting digital platform interface for the educational company "Acecerty". Generate a high-fidelity design layout using strict Auto Layout, clean layer groups, and component sets with explicit variants built for advanced prototyping transitions and Next.js / Framer Motion / GSAP implementation.

### 1. Universal Brand Token & Universal Color Mapping System
You must implement a strict, dual-theme token paradigm across every single page layout, structural section, module, block, and sub-component generated to facilitate flawless dynamic switching:
- **Dark Theme Configuration (Default State):** Set the canvas base background to deep, pure blacks (#000000 / #050505). All text layers, structural header groups, navigation links, and brand logo vectors must utilize high-contrast pure white or crisp off-white typography (#FFFFFF / #F5F5F7).
- **Light Theme Configuration:** Parallel to the dark layout, map out matching structural frame variations shifting the canvas base to a bright neutral background (#FFFFFF / #FAF9F6). Crucially, invert all typography, section titles, headers, navigation text, and structural logo fills/strokes to high-visibility dark charcoal or rich black tones (#111111 / #1A1A1A). Every element must map to light-and-dark value pairs to guarantee headers and logos remain fully visible and structurally sharp during live theme swaps.
- **Brand Accent Color Integration:** Extract and lock down the signature vibrant teal/cyan color (HEX: #00A2B6) as the core accent token. Apply this color universally across both light and dark themes for focus states, high-priority primary CTA buttons, dynamic text carets, card hover borders, and active indicators.

### 2. Global Layout Structure & Page Sections
- **Persistent Header & Navigation Bar:** Build an absolute or sticky top navigation zone containing structural vector logo marks, flexible text links, and a dedicated layout slot for a theme-switching element. All logo weights and link boundaries must fluidly map to color inversion styles to guarantee perfect visibility on theme state flips.

- **Hero Section (Interactive 3D Video & Constellation Canvas):** Build a multi-layered cinematic viewport canvas optimized via distinct breakpoint scaling models for desktop and mobile form factors.
  - *The Background Plane:* Hosts an absolute-positioned, low-latency HTML5 2D background canvas executing the high-performance network node constellation engine (defined in System K), casting interactive, physics-driven particle matrices behind all foreground objects.
  - *The Core Asset Plane (3D Live Video):* Embedded directly into the visual layout is a premium, looping 3D live-rendered video element styled to blend seamlessly into the website's theme parameters. The video asset must structurally respond to the universal color system: utilizing alpha-channel background transparency or overlay blending passes (such as screen or mix-blend-exclusion) to remain cleanly integrated whether the theme shifts from pure dark canvas tones to bright light-mode variants.
  - *Desktop Scale & Layout Optimization:* On screen widths $\ge$ 1024px, the layout organizes into a high-fidelity, dual-column horizontal flex row (or a layered full-bleed absolute structure) occupying exactly `100vh`. Text blocks, badge arrays, and high-priority primary CTA elements scale to cinematic dimensions (e.g., header text tracking at `text-5xl` to `text-8xl` with tracking-tighter margins). The 3D live video is positioned as a dominant, high-resolution focal frame (`w-1/2` or full-bleed `object-cover`), retaining an active mouse-proximity tracking field constraint clamped precisely to a 200px radius.
  - *Mobile Scale & Layout Optimization:* On screen widths $\le$ 768px, the section automatically refactors into a stacked vertical flex column layout to preserve readability and single-handed tap ergonomics above the fold. Typography layers fluidly downscale to balanced, high-contrast proportions (`text-4xl` or `text-5xl`), stacking clean information layers cleanly above or directly below the asset container. The 3D live video container dynamically shifts its geometry to an optimized 16:9 or 4:3 responsive aspect ratio to eliminate awkward layout cropping, while the interactive constellation node background seamlessly downscales particle density factors and trades tracking-pointer coordinates for automated touch-impact displacement fields to guarantee perfect layout stability.

- **Course Library & Dynamic Media Center:** Formulate comprehensive responsive visual showcase layouts tracking educational paths and course profiles using advanced card frameworks.
- **Scroll-Driven Sticky Showcase (Below Courses):** Allocate a dedicated layout section directly below the Course Center configured for a pinned, vertical viewport layout block. This zone handles a sequence of full-bleed visual cards overlapping one another through scroll interactions to unveil detailed feature deep-dives or learning milestones.
- **Integration & Features Section:** Create a dedicated scroll-container zone below the fold designed to reveal structural course paths and platform integrations utilizing scroll-driven visual choreography.

### 3. Integrated Component Sets & Animation Framework
You must explicitly generate separate Component Sets with distinct design variants to support eleven native interactive animation frameworks:

#### System A: Theme Toggle Button & View Transitions (Reference: Skiper4 & skiper26 (1).tsx)
Create a standalone "Theme Switcher Button" Component Set combining full-page View Transitions with high-fidelity button micro-interactions.
- **Button Micro-Animations (Skiper4):** Design the visual toggle button based on the Framer Motion SVG morphing logic found in "Skiper4". Provide distinct variants for the button states (e.g., a sun vector morphing into a crescent moon using animated clipPath and rotate properties, or the path-drawing lightbulb toggle). The button must cleanly switch its background contrast (e.g., white on black, black on white) based on the active state.
- **Ultra-Smooth View Transition Layering (skiper26):** Clicking this selector must call the native document.startViewTransition layout API to seamlessly run a circular masking expansion morphing the viewport canvas. Ensure that while the background mask scales out, headers, navigation nodes, and logo vectors execute a highly synchronized alpha opacity cross-fade (blending fills and stroke properties seamlessly over a 300ms window) to prevent jumping, structural drop-outs, or layout flashing.

#### System B: Conditional Modifier Mouse Follower Overlay (Reference: skiper61 (1).tsx)
Design a highly unique mouse-tracking spotlight component layer whose tracking states are strictly constrained by a keyboard modifier condition:
- **Spotlight Frame Architecture:** Generate an independent canvas tracking component housing a soft, diffused radial gradient layer utilizing the brand accent cyan (#00A2B6) shifting smoothly to an alpha-transparent outer edge.
- **Conditional Key Modifier Logic:** Map three explicit structural variants for the canvas cursor lifecycle:
  - *State 1 (Modifier Idle):* When moving the pointer normally across the viewport, the tracker element must reside at Opacity: 0% and Scale: 0. It remains completely disabled and sleeping.
  - *State 2 (Modifier Active - Key Trigger):* Listen for a global window keyboard event. Only when the "Ctrl" key is actively held down (keydown / e.ctrlKey === true), the component shifts to Opacity: 100% and scales cleanly to Scale: 1. 
  - *Tracking Spring Constants:* While "Ctrl" is maintained, the tracking container must trail the active pointer coordinates using tight, responsive low-inertia spring parameters configured with a Mass of 0.1, Damping value of 10, and Stiffness rating of 131. The instant the "Ctrl" key is released, it scales down to 0 and disappears.

#### System C: Accordion-Style Course Showcase Carousel (Reference: skiper52.tsx)
Instead of a standard flat course grid, arrange the parsed legacy course items in a horizontal flex-row layout container. Create a dedicated Component Set named "Showcase Card" featuring two explicit design states:
- **Variant 1: Collapsed Card:** A narrow vertical frame constrained to a width of '5rem' (80px) and a height of '24rem' (384px) with a fully rounded corner radius of 24px. This card is a minimalist layout displaying only a condensed vertical course tag or border identifier.
- **Variant 2: Expanded Card:** A wide, focal container expanding fluidly to a width of '24rem' (384px) and a height of '24rem' (384px) with a 24px corner radius. It must reveal a rich background course thumbnail image, an absolute-positioned dark gradient overlay mask, a high-contrast course metadata title block, pricing metrics, and an accent CTA badge.

#### System D: Micro-Interactive Vector Icons (Reference: skiper99 (1).tsx)
Build specialized UI component sets for page actions, anchor states, and navigation systems utilizing the interactive vector properties defined in "hover/click maps".
- **Arrow/Chevron Link Icon:** Build an interactive arrow component containing two states. State 1 (Idle): A single sharp ChevronRight vector icon with a hidden internal line vector layer set to Scale-X (0%). State 2 (Hover): The ChevronRight shifts horizontally by a minor offset space while the vector line scales up fluidly to Scale-X (100%), creating a dynamic animated arrow link.
- **Morphing Hamburger Menu Icon:** Generate a navigation "Menu Icon" Component Set. State 1 (Default): Three absolute-positioned horizontal vector path lines stacked evenly with 5px geometry gaps. State 2 (Active/Toggled Toggle): The center path layer collapses to 0% opacity while the top and bottom paths translate to the true center point and cross-rotate at 45-degree and -45-degree angles to generate a crisp geometric 'X' close indicator.
- **Reactive Audio Volume Icon:** For inline course video previews, create a multi-state volume speaker vector button. Include structural design states for an active audio state (revealing layered concentric stroke rings) and a muted state (where an absolute-positioned diagonal line strike-through layer scales from 0 to 1 over the icon face alongside a subtle rotation tilt).

#### System E: Smooth Caret Input Fields (Reference: Skiper106 / SmoothInput)
Design an ultra-premium input field component set for the platform's search bars and login portal inputs to replace standard native browser inputs with fluid typing physics.
- **Component Geometry:** Input containers should be styled with a comfortable 16px padding (p-4), a 16px corner radius (rounded-2xl), and an internal grid structure that hides native caret visibility (caret-transparent).
- **Variant 1: Unfocused State:** Displays a faint outline and subtle placeholder text layer set to 40% opacity. The interactive caret is hidden.
- **Variant 2: Focused / Typing State:** Triggered when active. The container gains a 2px high-contrast focus outline using the brand accent color (#00A2B6). A dedicated vertical line vector element representing the text caret layer emerges in the signature cyan color block (#00A2B6), designed to smoothly track immediately following the character layout spacing.
- **Prototyping Setup:** Ensure the custom caret bar is a separated absolute layer within the internal Auto Layout structure to easily simulate spring animation tracking behaviors (stiffness of 500, damping of 30, and mass of 0.5) during character addition micro-transitions.

#### System F: Scroll-Driven Cinematic Convergence Layers (Reference: skiper31 (1).tsx & skiper31 (2).tsx)
Incorporate a high-impact scroll-driven layout strategy modeled precisely on "skiper31 (1).tsx" and "skiper31 (2).tsx" that transforms flat, static typography strings and tech-stack icon elements into dynamic kinetic components triggered by viewport scroll tracking metrics (e.g., Lenis).
- **Character Splitting Layout (CharacterV1):** For large segment typography headers, programmatically split the text string into individual absolute or inline-block character layers. Define two distinct operational scroll target states:
  - *Variant 1 (Scroll Origin 0.0):* Characters are exploded outwards across the horizontal canvas using X-axis multipliers proportional to their index distance from the true text string center index, combined with an aggressive 3D perspective distortion plane (rotateX driven by center offsets).
  - *Variant 2 (Scroll Target 0.5):* As viewport scrolling progresses smoothly down to 50%, the letters converge symmetrically back to center tracking coordinates, flattening rotation down to 0 degrees to form a crisp, unified tracking-tight display headline block.
- **Icon / Image Vector Scattering (CharacterV2 & CharacterV3):** For integration partner layout rows, separate tech stack software icon images into individual nodes. Establish explicit layout states mapping scroll progress paths:
  - *State 1 (Scattered Chaos):* Images are dynamically displaced outward from the central vector coordinates along both X and Y geometric layout tracks using distance offsets (e.g., Math.abs(distanceFromCenter) * 50), scaled down to 75%, and skewed using localized center-index rotation forces.
  - *State 2 (Unified Cluster Grid):* As scroll boundaries transition from 0 to 0.5, all floating integration vector thumbnails fluidly gravitate inward, automatically resetting scale properties to 100% and stabilizing their rotation planes to lock flat into an aligned, highly organized rows-and-columns flex layout or structural ribbon tracking container.

#### System G: Scroll-Driven Vector Path Trajectory (Reference: skiper19.tsx)
Implement a continuous, structural storytelling element across multi-fold layouts using a dynamic scroll-drawn vector system modeled on "skiper19.tsx".
- **Abstract Vector Track Layer:** Generate a large, winding, organic SVG curve vector layer set to absolute positioning (absolute Z-0). This line must track background layouts fluidly, weaving directly behind or around primary typography blocks and grid boundaries.
- **Stroke Progress States:** Establish an explicit layout path blueprint configured for Framer Motion's pathLength properties.
  - *Variant 1 (Scroll Zero):* The vector stroke is invisible or partially drawn (pathLength: 0.5 or strokeDashoffset maxed), hiding its path lines away from view.
  - *Variant 2 (Scroll Final / Complete 1.0):* As the viewport scroll depth increases across a prolonged tracking section (simulated on canvas layers up to 350vh), the path line seamlessly grows, filling its stroke contours dynamically in the brand accent teal color (#00A2B6) to pull the visitor's attention down toward landing elements.
- **Content Overlay Blocks:** Position premium text containers over the line track, structuring clean typography frames that alternate backgrounds smoothly between states (e.g., transitioning blocks cleanly into deep dark-theme backgrounds like #1F3A4B or canvas tones).

#### System H: Immersive Clip-Path Video Player Pop-Over (Reference: skiper67.tsx)
Build a modular video player component library dedicated to the Hero Section video reel and inline Course Library cards to create a fluid preview-to-modal workflow using the design systems modeled in "skiper67.tsx".
- **Inline Preview Card Component:** Create a container housing a looping background video file. Set an interactive tracking behavior over this block: when a pointer hovers inside, a localized contextual text node reading "Play" alongside a micro vector play arrow emerges. This floating tag must track mouse coordinate positions using low-inertia springs (mass: 0.1) and utilize an absolute mix-blend-exclusion rendering pass to stay legibly inverted against changing background video pixels.
- **Immersive Overlay Container Component Set:** Define a standalone modal interface overlay component that renders over the standard page layouts upon clicking the inline preview.
  - *Variant 1 (Modal Backdrop Grid):* An absolute screen layer spanning full width/height (100vw/100vh) holding a premium backdrop-blur-lg layout mask layered on a background opacity block (bg-background/90). Includes a discrete close action element in the top right consisting of a vector cross button rotated exactly 45 degrees.
  - *Variant 2 (Cinematic Clip-Path Frame - Opening State):* The video node container begins masked tightly to a compact center geometry using a specific clipping mask constraint (clipPath: "inset(43.5% 43.5% 33.5% 43.5%)") at 0% initial visibility.
  - *Variant 3 (Cinematic Clip-Path Frame - Expanded Target):* The mask spring-unfolds fluidly over a structural timeline (preset to a spring stiffness of 100 and damping of 20) until it matches a widescreen video aspect-ratio layout (clipPath: "inset(0% 0% 0% 0%)") at 100% opacity.
- **Minimalist Audio/Video Control Bar Component:** Attach a premium controls track absolute-positioned to the bottom center axis of the video container. Keep the interface clean and stark using minimalist, borderless control assets mapped directly to custom chrome states: a vector play/pause toggle, an inline flat timeline tracker line (MediaTimeRange), and a responsive audio speaker indicator.

#### System I: HTML5 Canvas Ambient Sprite Crowd Engine (Reference: Skiper39)
Incorporate a high-fidelity dynamic crowd background simulation component to run beneath secondary site content sections, matching the structure and logic detailed in "Skiper39".
- **Canvas Viewport Architecture:** Provision an absolute-positioned HTML5 `<canvas>` component occupying a bottom-aligned frame mapping to '90vh' or full section boundaries. The canvas element must utilize high-performance requestAnimationFrame tracking loops linked to GSAP's native core ticker (`gsap.ticker`).
- **Dynamic Sprite Subdivision Logic:** Define a system parsing a structured multi-character composite image sprite asset mapped to strict row and column matrices (configured default parameters: 15 rows by 7 columns). The code must loop programmatically to cleanly separate individual sprite boundaries into unique, isolated structural data slices ("Peeps").
- **Kinetic Walk Physics & Axis Animation:** Implement automated generation functions assigning independent horizontal walking tracks to active sprite particles:
  - *Horizontal Translation:* Utilizing linear GSAP tweens, move individual sprite nodes completely across the horizontal stage tracking randomly assigned inverted entry coordinates (from left to right or right to left). Scale-X matrices must instantly toggle (-1 vs 1) to mirror vector geometry toward the moving direction.
  - *Vertical Bobbing Physics:* Simultaneously layer a highly synchronized, high-frequency yoyo animation path shifting the node's vertical Y-coordinates upwards by 10px to simulate organic rhythmic footsteps. Randomize structural time-scale factors (range constants: 0.5 to 1.5) on every active timeline loop to establish varied pacing metrics across the view field.
- **Depth Sorting & Z-Index Management:** Maintain an active structural array tracker tracking active characters. Continually parse the rendering priority sequence by sorting layer values against localized vertical anchors (`anchorY`). Elements traveling along lower screen coordinates must automatically drop behind foreground targets to build clear visual depth landscapes on canvas redraw frames.
- **Responsive Screen Tracking Reinitialization:** Bind listener events to window resizing cycles. Upon triggering viewport adjustments, immediately kill all active instance tweens, clear current character index memory stacks, and reset scaling matrices to prevent sprite blurring or layout breakdown.
- **Theme Mask Layering Compatibility:** Ensure the underlying canvas rendering loops support alpha transparency. Apply a soft backdrop composition layer so that when changing interface themes (Light/Dark states via System A), the active background crowd visibility smoothly shifts contrast rules without dropping animation frames.

#### System J: Scroll-Driven Pinned Card Stack Showcase (Reference: Skiper17)
Implement a premium, vertical scroll-pinned layered card stacking feature module positioned immediately below the primary Course Library to handle structural deep-dives, modeling the exact physics mechanics from "Skiper17".
- **Viewport Locking & Pin Execution:** Design a structural scroll section frame containing a target timeline container (`.sticky-cards`). Integrate GSAP `ScrollTrigger` rules to pin the module entirely to the viewport top (`pin: true`, `start: "top top"`). Extend the overall physical scroll track height proportional to the number of rendered layers using direct window bounds (e.g., `+=${window.innerHeight * (totalCards - 1)}`), operating with clean, tight layout scrub parameters (`scrub: 0.5`, `pinSpacing: true`).
- **Card Element Layer Stacking Array:** Group a multi-item collection of visual data cards structured with comfortable border-radius bounds (`rounded-4xl`) inside a centered layout bounding frame. Position every card layer absolutely over one another (`absolute h-full w-full object-cover`).
- **Sequential Timeline Transformation Loops:** Build a structural GSAP animation timeline that translates layers smoothly as viewport scroll depth trends forward:
  - *Initial Setup State:* Force the base layout instance (Index 0) to standard focal dimensions (`y: "0%", scale: 1, rotation: 0`). Force all subsequent overlapping cards (Index 1 and beyond) to rest directly below the visible layout viewport boundary (`y: "100%", scale: 1, rotation: 0`).
  - *Active-to-Exit Transformation:* As the timeline scrubs forward, scale the active card layer down to `scale: 0.7` and apply an organic, premium spatial angle offset of `rotation: 5` using an effortless ease linear trajectory (`ease: "none"`).
  - *Entrance Transition Layer:* Symmetrically coordinate the oncoming stacked image card to translate vertically upward from `y: "100%"` straight to a locked `y: "0%"` coordinate frame to layer perfectly over the previous scaled-down layout slice.
- **Global Theme Synchronization:** Ensure the background borders, image filters, text annotations, or content tags applied on top of these cards are tied natively to your dynamic color inversion systems so text elements maintain AA contrast on both dark theme and light theme toggles.

#### System K: Hero Backing Interactive Particle Constellation Engine (Reference: AetherFlowHero)
Incorporate a high-performance interactive network mesh onto the primary Hero Section background canvas using HTML5 2D Context scripting architecture modeled precisely on the structure of `AetherFlowHero`.
- **Canvas Base & Real-time Sizing:** Inject an absolute-positioned canvas element (`absolute top-0 left-0 w-full h-full`) running inside a `requestAnimationFrame` render loop (`animate`). Bind calculation updates to global window resize events to guarantee crisp, edge-to-edge browser field rendering dimensions on scale variations.
- **Constellation Node Lifecycle Framework:** Formulate an internal `Particle` blueprint managing localized tracking metrics: coordinate positions (`x, y`), directional velocities (`directionX, directionY`), dimension values (`size` ranging randomly between 1px and 3px), and alpha stroke colors. Initialize particle densities scaled directly against viewport area equations (e.g., `(canvas.height * canvas.width) / 9000`).
- **Dynamic Boundary Collisions & Inter-Particle Connectors (`connect`):** - *Edge Detection:* Particles must track boundaries perfectly. If coordinates hit outer width/height canvas perimeters, invert velocity matrices instantly to create recursive bouncing behaviors.
  - *Proximity Linkage:* Calculate distance arrays between all alive particles via Pythagorean theorem loops. When vector distances pass below neighborhood proximity rules, draw elegant mathematical proximity vectors (`ctx.moveTo` to `ctx.lineTo`) bridging coordinates together at a single pixel line weight (`ctx.lineWidth = 1`).
- **Kinetic Mouse Distortion Fields & Proportional Pushing:** Map an internal listener tracker tracing pointer positions (`mousemove`, `mouseout`). Define a customized repulsion boundary radius parameter (clamped exactly to `radius: 200` on Desktop viewports). When a node falls within the pointer's active coordinate footprint area, compute normalized force directions and apply an inverse distance scalar equation (`(mouse.radius - distance) / mouse.radius`) to smoothly deflect nodes away from the cursor path by an active step multiplier of 5 to simulate fluid compression. For touch-active mobile displays, automatically scale down the detection radius and replace pointer events with touch positions or ambient velocity transitions to maintain flawless rendering speed.
- **Responsive Dual-Theme Color Inversion Architecture:** Replace all hardcoded, single-theme background fills and static color strings inside the animation rendering loops with dynamic layout variables wired into **System A**:
  - *Dark Theme Active State:* The render loop must paint the canvas background layer into standard deep blacks (`ctx.fillStyle = 'black'`). Set un-hovered network connecting strokes to deep glowing amethysts (`rgba(200, 150, 255, opacity)`), bright vector particle nodes to brilliant purples (`rgba(191, 128, 255, 0.8)`), and dynamically shift links entering the active mouse radius to clear white parameters (`rgba(255, 255, 255, opacity)`).
  - *Light Theme Active State:* The canvas clear-rect frame must smoothly switch its draw fill base to bright neutral whites (`ctx.fillStyle = '#FFFFFF'`). Invert network line connections to crisp, translucent low-contrast slate gray paths (`rgba(100, 110, 125, opacity)`), map standard particle nodes to rich brand cyan accents (`rgba(0, 162, 182, 0.8)`), and force local mouse-hover lines to lock into high-visibility charcoal strings (`rgba(17, 17, 17, opacity)`) so node grids remain pristine and high-contrast on both toggle configurations.

Deliver this as a pristine, fully editable, and unified setup with highly organized layer trees and strict color systems ready for immediate code generation.